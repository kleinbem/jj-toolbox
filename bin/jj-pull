#!/usr/bin/env bash
# jj-pull — fetch from origin and rebase the working-copy change onto
# trunk(). Runs against whatever jj repo you're cwd'd into.
set -uo pipefail
source "$(dirname "${BASH_SOURCE[0]}")/lib.sh"

jj_use_gh_credential_helper

jj git fetch || exit 1
jj rebase -d 'trunk()'
